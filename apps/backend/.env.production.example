# Production Environment Variables Template
# Copy this file to .env.production and set actual production values
# NEVER commit actual production secrets to git!

NODE_ENV=production

# App Configuration
PORT=8080
LOG_LEVEL=warn

# Database (Production PostgreSQL - set these in your deployment environment)
# DATABASE_URL="postgresql://username:password@prod-host:5432/database_name"
# DIRECT_URL="postgresql://username:password@prod-host:5432/database_name"

# Redis (Production Redis - set these in your deployment environment)
# REDIS_URL="redis://prod-redis-host:6379"

# Supabase (Production instance - set these in your deployment environment)
# SUPABASE_URL="https://your-prod-project.supabase.co"
# SUPABASE_SERVICE_ROLE_KEY="your-production-service-role-key"

# JWT Secret (Production - MUST be set in deployment environment)
# JWT_SECRET="your-super-secure-production-jwt-secret"

# CORS Configuration (Production domain)
# CORS_ORIGIN="https://your-production-domain.com"

# Production Features
ENABLE_DEBUG_LOGS=false
ENABLE_CORS=true

# Security
TRUST_PROXY=true
RATE_LIMIT_ENABLED=true

# Monitoring (optional - configure if using monitoring services)
# SENTRY_DSN="your-sentry-dsn"
# NEW_RELIC_LICENSE_KEY="your-newrelic-key"