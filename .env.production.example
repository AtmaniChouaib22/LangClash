# Production Environment Template
# Copy this to .env.production and update with real values

# ======================
# PRODUCTION ENVIRONMENT
# ======================

# Database (Production PostgreSQL - use hosted service like Railway, Supabase, AWS RDS)
DATABASE_URL="postgresql://prod_user:secure_password@your-prod-db-host:5432/langclash_prod"
DIRECT_URL="postgresql://prod_user:secure_password@your-prod-db-host:5432/langclash_prod"

# Redis (Production Redis - use hosted service like Redis Cloud, AWS ElastiCache)
REDIS_URL="redis://your-prod-redis-host:6379"

# Supabase (Same project, different keys for production)
SUPABASE_URL="https://iosncykojghxujgbzjtj.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-production-service-role-key"

# App URLs (Your actual domain)
NEXT_PUBLIC_APP_URL="https://langclash.com"
NEXT_PUBLIC_SOCKET_URL="https://api.langclash.com"

# JWT Secret (Use a strong, unique secret for production)
JWT_SECRET="super-secure-jwt-secret-for-production-change-this"

# Node Environment
NODE_ENV="production"

# Additional Production Settings
PORT=3001
LOG_LEVEL="error"
CORS_ORIGIN="https://langclash.com"